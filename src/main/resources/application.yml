spring:
  data:
    web:
      pageable:
        one-indexed-parameters: true
    mongodb:
      authentication-database: admin
      username: root
      password: root
      database: pocmongo
      host: localhost
      port: 27017
      uuid-representation: standard
  output:
    ansi:
      enabled: ALWAYS

  logging:
    level:
      org:
        springframework:
          data:
            mongodb:
              core:
                MongoTemplate: DEBUG